<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.AttachmentMapper">

    <insert id="insert" parameterType="com.example.entity.Attachment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO doc_attachment (id, file_path, file_type, creator, create_date, updater, update_date, file_name)
        VALUES (#{id}, #{filePath}, #{fileType}, #{creator}, #{createDate}, #{updater}, #{updateDate}, #{fileName})
    </insert>

</mapper>
